server:
  port: 8081

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/davidSanti17/Config-Server-Example.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASS}